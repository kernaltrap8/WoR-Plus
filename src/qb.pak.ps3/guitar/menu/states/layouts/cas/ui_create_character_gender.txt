Unknown [1c00000000000000000004080000000000000000]Script cas_gender_focus_change 0x0d49ecb1 [	${StructQBString Instrument = cas_default_instrument}$	:i if $StructureContains$${			StructQBString structure = cas_current_appearance			StructQBKey CAS_Full_Body		}$				:i if (((~$cas_current_appearance$->$CAS_Full_Body$)->$desc_id$) = $Avatar$)			:i $existing_avatar$ = %i(1)		:i endif	:i endif	:i if $GotParam$${StructQBKey avatar_type}$		:i if $GotParam$${StructQBKey existing_avatar}$			:i if $IsAvatarFemale$$avatar_meta_data$ = ((~$cas_current_appearance$->$CAS_Full_Body$)->$avatar_meta_data$)				:i if (%GLOBAL%$avatar_type$ = $avatar_female$)					:i return 				:i endif			:i else 				:i if (%GLOBAL%$avatar_type$ = $avatar_male$)					:i return 				:i endif			:i endif		:i endif		:i $change$$cas_current_new_char_type$ = %GLOBAL%$avatar_type$	:i else 		:i if NOT $GotParam$${StructQBKey existing_avatar}$			:i $get_is_female_from_appearance$${StructQBString appearance = cas_current_appearance}$			:i if (%GLOBAL%$new_desc_id$ = $GH4_CAR_Female$)				:i if (%GLOBAL%$is_female$ = %i(1))					:i return 				:i endif			:i else 				:i if (%GLOBAL%$is_female$ = %i(0))					:i return 				:i endif			:i endif		:i endif		:i if (%GLOBAL%$new_desc_id$ = $GH4_CAR_Female$)			:i $change$${StructQBKey cas_current_new_char_type = Female}$		:i else 			:i $change$${StructQBKey cas_current_new_char_type = Male}$		:i endif	:i endif	:i $generate_random_genre$	:i $random_character_throttled$$char_type$ = (~$cas_current_new_char_type$)$Instrument$ = %GLOBAL%$Instrument$$genre$ = %GLOBAL%$genre$	:i endfunction]Script random_character_throttled 0x47a240bf [	:i if $ScriptIsRunning$${StructQBKey random_character_throttled_spawned}$		:i $do_wait$ = %i(1)	:i endif	:i $KillSpawnedScript$${StructQBKey name = random_character_throttled_spawned}$	:i $spawnscriptnow$$random_character_throttled_spawned$$params$ = isNull 	:i endfunction]Script random_character_throttled_spawned de4bdb64 [	${StructQBKey Instrument = None}$	:i if (~$leaving_genre_select$ = %i(0))		:i if $GotParam$${StructQBKey do_wait}$			:i $wait$${				StructInt 5				StructQBKey gameframes			}$					:i endif	:i endif	:i if ((%GLOBAL%$char_type$ = $avatar_male$) OR (%GLOBAL%$char_type$ = $avatar_female$))		:i if (%GLOBAL%$char_type$ = $avatar_female$)			:i $wanted_is_female$ = %i(1)		:i else 			:i $wanted_is_female$ = %i(0)		:i endif		:i $generate_random_name$$is_female$ = %GLOBAL%$wanted_is_female$		:i $modify_custom_profile_fullname$$id$ = (~$cas_current_profile$)$fullname$ = %GLOBAL%$random_name$$savegame$ = (~$cas_current_savegame$)		:i $generate_random_avatar$$is_female$ = %GLOBAL%$wanted_is_female$		:i if (~$leaving_genre_select$ = %i(1))			:i return 		:i endif		:i $cas_queue_add_request$$appearance$ = (~$cas_current_appearance$)$player$ = (~$cas_current_player$)$Instrument$ = %GLOBAL%$Instrument$%GLOBAL%$cas_flag$	:i else 		:i if (%GLOBAL%$char_type$ = $Female$)			:i $wanted_is_female$ = %i(1)		:i else 			:i $wanted_is_female$ = %i(0)		:i endif		:i $cas_get_is_female$$player$ = (~$cas_current_player$)		:i if (($GotParam$$genre_select$) AND (%GLOBAL%$wanted_is_female$ = %GLOBAL%$is_female$))			:i $cas_set_random_appearance$$savegame$ = (~$cas_current_savegame$)$is_female$ = %GLOBAL%$is_female$$genre$ = %GLOBAL%$genre$$new_car_character$$random_instruments$			:i if (~$leaving_genre_select$ = %i(1))				:i return 			:i endif			:i $cas_queue_add_request$$appearance$ = (~$cas_current_appearance$)$player$ = (~$cas_current_player$)$Instrument$ = %GLOBAL%$Instrument$$in_cas$		:i else 			:i $cas_change_current_character_gender$$Instrument$ = %GLOBAL%$Instrument$$is_female$ = %GLOBAL%$wanted_is_female$		:i endif	:i endif	:i if (~$leaving_genre_select$ = %i(1))		:i return 	:i endif	:i $cas_queue_wait$	:i if $GotParam$${StructQBKey anim}$		:i if $GetCurrentCASObject$			:i $Band_PlayAnim$$name$ = %GLOBAL%$cas_object$$anim$ = %GLOBAL%$anim$$no_wait$		:i endif	:i endif	:i $wait$${		StructInt 5		StructQBKey gameframes	}$		:i endfunction]