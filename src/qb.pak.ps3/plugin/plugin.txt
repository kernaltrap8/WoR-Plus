Unknown [1c000000000000000000045c0000000000000000]Script Plugin_Quickview f38eaeb4 [	:i $spawnscriptnow$$Plugin_Quickview_Spawned$$params$ = isNull 	:i endfunction]Script Plugin_Quickview_Spawned 0x48c06d6a [	:i $printstruct$ isNull 	:i $stoprendering$	:i $FinishRendering$	:i $wait$${		StructInt 3		StructQBKey gameframes	}$		:i $KillTextureSplats$${StructQBKey all}$	:i $PreAllocSplats$	:i switch %GLOBAL%$export_type$		:i case $scene$			:i $scriptassert$${StructQBStringQs 0x179af63d}$			:i endcase		case $sky$			:i switch %GLOBAL%$project$				:i case $gun2$					:i $TOD_Change_Sky$$sky$ = %GLOBAL%$sky$					:i endcase				default 					:i $Change_Sky$$sky$ = %GLOBAL%$sky$			:i end_switch			:i endcase		case $zone$			:i switch %GLOBAL%$project$				:i case $delta$					:i $GetPakManCurrentName$${StructQBKey map = zones}$					:i $zone_name$ = %GLOBAL%$pakname$					:i $FormatText$$textname$ = $zone$^[a2f2c278]^$z$ = %GLOBAL%$zone$$DontAssertForChecksums$					:i if NOT (%GLOBAL%$zone_name$ = %GLOBAL%$zone$)						:i $JumpToZone$$zone$ = %GLOBAL%$zone$					:i else 						:i $BlockPendingPakManLoads$${StructQBKey map = zones}$						:i $RefreshCurrentZones$					:i endif					:i endcase				case $gamma$					:i if NOT (%GLOBAL%$zone$ = $viewer$)						:i $FormatText$$checksumname$ = $zone$%s("load_%z")$z$ = %GLOBAL%$zone$$DontAssertForChecksums$					:i endif					:i $GetCurrentLevel$					:i if NOT (%GLOBAL%$level$ = %GLOBAL%$zone$)						:i $SetPakManCurrentBlock$${							StructQBKey map = zones							StructQBKey pak = None						}$												:i $change_level$$level$ = %GLOBAL%$zone$					:i else 						:i $SetPakManCurrentBlock$${							StructQBKey map = zones							StructQBKey pak = None						}$												:i $change_level$$level$ = %GLOBAL%$zone$					:i endif					:i endcase				default 					:i if NOT (%GLOBAL%$zone$ = $viewer$)						:i $FormatText$$checksumname$ = $zone$%s("load_%z")$z$ = %GLOBAL%$zone$$DontAssertForChecksums$					:i endif					:i $GetCurrentLevel$					:i $RefreshPakManSizes$${StructQBKey map = zone}$					:i if NOT (%GLOBAL%$level$ = %GLOBAL%$zone$)						:i $SetPakManCurrentBlock$${							StructQBKey map = zones							StructQBKey pak = None						}$												:i $change_level$$level$ = %GLOBAL%$zone$					:i else 						:i $BlockPendingPakManLoads$${StructQBKey map = zones}$						:i $RefreshCurrentZones$					:i endif			:i end_switch			:i endcase		case $viewer$			:i switch %GLOBAL%$project$				:i case $delta$					:i $GetMissionName$					:i if NOT (%GLOBAL%$mission$ = $viewer$)						:i $load_new_mission$$mission_pak$ = %GLOBAL%$viewer$$reload_zones$					:i else 						:i $BlockPendingPakManLoads$${StructQBKey map = zones}$						:i $RefreshCurrentZones$					:i endif					:i endcase				case $gh5$					:i $printf$${StructQBStringQs a93a6f80}$					:i $RefreshPakManSizes$${StructQBKey map = zones}$					:i $debug_select_venue$${						StructInt norestart = 1						StructQBKey level = viewer					}$										:i if $NodeFlagExists$${StructQBKey nodeflag_viewerlights}$						:i $ChangeNodeFlag$${							StructQBKey nodeflag_viewerlights							StructInt 0						}$												:i $ChangeNodeFlag$${							StructQBKey nodeflag_viewerlights							StructInt 1						}$											:i endif					:i $printf$${StructQBStringQs 0x82cc91b0}$					:i endcase				default 					:i $GetCurrentLevel$					:i $RefreshPakManSizes$${StructQBKey map = zones}$					:i if NOT (%GLOBAL%$level$ = $viewer$)						:i $SetPakManCurrentBlock$${							StructQBKey map = zones							StructQBKey pak = None						}$												:i $change_level$${StructQBKey level = viewer}$					:i else 						:i $BlockPendingPakManLoads$${StructQBKey map = zones}$						:i $RefreshCurrentZones$					:i endif			:i end_switch			:i endcase		case $objviewer$			:i switch %GLOBAL%$project$				:i case $gh5$					:i $printf$${StructQBStringQs 0x81b18b01}$					:i $debug_select_venue$${						StructInt norestart = 1						StructQBKey level = load_z_objviewer					}$										:i $printf$${StructQBStringQs 0x1ed2cd1f}$					:i endcase				default 					:i $GetCurrentLevel$					:i if NOT (%GLOBAL%$level$ = $load_z_objviewer$)						:i $SetPakManCurrentBlock$${							StructQBKey map = zones							StructQBKey pak = None						}$												:i $change_level$${StructQBKey level = load_z_objviewer}$					:i else 						:i $BlockPendingPakManLoads$${StructQBKey map = zones}$						:i $RefreshCurrentZones$					:i endif			:i end_switch			:i endcase		case $mission$			:i switch %GLOBAL%$project$				:i case $gun2$					:i $printf$${StructQBStringQs 0x625a9d67}$					:i $load_new_mission$$mission_pak$ = %GLOBAL%$mission$$reload_zones$					:i endcase				default 					:i $BlockPendingPakManLoads$${StructQBKey map = zones}$					:i $RefreshCurrentZones$			:i end_switch			:i endcase		case $Model$			:i $change$${StructInt view_mode = 0}$			:i $ToggleViewMode$			:i $ToggleViewMode$			:i $ToggleViewMode$			:i $exit_view_models_menu$			:i $toggle_model_viewer$			:i $view_model$$modelname$ = %GLOBAL%$Model$			:i endcase		case $Skin$			:i $change$${StructInt view_mode = 0}$			:i $ToggleViewMode$			:i $ToggleViewMode$			:i $ToggleViewMode$			:i $exit_view_models_menu$			:i $toggle_model_viewer$			:i $view_model$${				StructQBKey profile = Ped_BatMasterson				StructQBKey animLoadScript = animload_ped_male				StructQBKey extraAnim = animload_ped_male				StructQBKey defaultanim = MV_Idle_Denton				StructQBKey SkeletonName = gun			}$						:i endcase		default 	:i end_switch	:i $startrendering$	:i $kill_panel_message_if_it_exists$${StructQBKey id = quickview}$	:i endfunction]